name: outreach-pages
on:
  pull_request:
    branches: [ main ]

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "build ok"

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR-only deploy ✓"
