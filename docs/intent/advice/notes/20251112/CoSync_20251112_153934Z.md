# CoSync — RickPublic · PR‑safe Pages

- when: 20251112_153934Z
- intent: keep branch protection requiring **deploy** while avoiding PR cancellations

## What changed
- Workflow now runs on `pull_request`.
- PRs **skip** Pages–specific steps (`configure-pages`, `upload-pages-artifact`, real publish).
- A single job named **deploy** runs on both PR and main:
  - PR → simulated no‑op step (green context).
  - main → real `actions/deploy-pages@v4` + live fingerprint verify.

## Verify
- On PR: check list shows a green **deploy** from GitHub Actions.
- On main: live page contains `build-fingerprint:`.
